/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#include <string.h>
#include "openni_disparity_msg_t.h"

static int __openni_disparity_msg_t_hash_computed;
static int64_t __openni_disparity_msg_t_hash;

int64_t __openni_disparity_msg_t_hash_recursive(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __openni_disparity_msg_t_get_hash)
            return 0;

    const __lcm_hash_ptr cp = { p, (void*)__openni_disparity_msg_t_get_hash };
    (void) cp;

    int64_t hash = 0x357bde4223b3c335LL
         + __int64_t_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
         + __int16_t_hash_recursive(&cp)
         + __int32_t_hash_recursive(&cp)
         + __byte_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __openni_disparity_msg_t_get_hash(void)
{
    if (!__openni_disparity_msg_t_hash_computed) {
        __openni_disparity_msg_t_hash = __openni_disparity_msg_t_hash_recursive(NULL);
        __openni_disparity_msg_t_hash_computed = 1;
    }

    return __openni_disparity_msg_t_hash;
}

int __openni_disparity_msg_t_encode_array(void *buf, int offset, int maxlen, const openni_disparity_msg_t *p, int elements)
{
    int pos = 0, thislen, element;

    for (element = 0; element < elements; element++) {

        thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].timestamp), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].width), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].height), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_encode_array(buf, offset + pos, maxlen - pos, p[element].pixel_offset, 2);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].disparity_data_nbytes), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __byte_encode_array(buf, offset + pos, maxlen - pos, p[element].disparity_data, p[element].disparity_data_nbytes);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int openni_disparity_msg_t_encode(void *buf, int offset, int maxlen, const openni_disparity_msg_t *p)
{
    int pos = 0, thislen;
    int64_t hash = __openni_disparity_msg_t_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __openni_disparity_msg_t_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int __openni_disparity_msg_t_encoded_array_size(const openni_disparity_msg_t *p, int elements)
{
    int size = 0, element;
    for (element = 0; element < elements; element++) {

        size += __int64_t_encoded_array_size(&(p[element].timestamp), 1);

        size += __int16_t_encoded_array_size(&(p[element].width), 1);

        size += __int16_t_encoded_array_size(&(p[element].height), 1);

        size += __int16_t_encoded_array_size(p[element].pixel_offset, 2);

        size += __int32_t_encoded_array_size(&(p[element].disparity_data_nbytes), 1);

        size += __byte_encoded_array_size(p[element].disparity_data, p[element].disparity_data_nbytes);

    }
    return size;
}

int openni_disparity_msg_t_encoded_size(const openni_disparity_msg_t *p)
{
    return 8 + __openni_disparity_msg_t_encoded_array_size(p, 1);
}

int __openni_disparity_msg_t_decode_array(const void *buf, int offset, int maxlen, openni_disparity_msg_t *p, int elements)
{
    int pos = 0, thislen, element;

    for (element = 0; element < elements; element++) {

        thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].timestamp), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].width), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].height), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int16_t_decode_array(buf, offset + pos, maxlen - pos, p[element].pixel_offset, 2);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].disparity_data_nbytes), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        p[element].disparity_data = (uint8_t*) lcm_malloc(sizeof(uint8_t) * p[element].disparity_data_nbytes);
        thislen = __byte_decode_array(buf, offset + pos, maxlen - pos, p[element].disparity_data, p[element].disparity_data_nbytes);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __openni_disparity_msg_t_decode_array_cleanup(openni_disparity_msg_t *p, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __int64_t_decode_array_cleanup(&(p[element].timestamp), 1);

        __int16_t_decode_array_cleanup(&(p[element].width), 1);

        __int16_t_decode_array_cleanup(&(p[element].height), 1);

        __int16_t_decode_array_cleanup(p[element].pixel_offset, 2);

        __int32_t_decode_array_cleanup(&(p[element].disparity_data_nbytes), 1);

        __byte_decode_array_cleanup(p[element].disparity_data, p[element].disparity_data_nbytes);
        if (p[element].disparity_data) free(p[element].disparity_data);

    }
    return 0;
}

int openni_disparity_msg_t_decode(const void *buf, int offset, int maxlen, openni_disparity_msg_t *p)
{
    int pos = 0, thislen;
    int64_t hash = __openni_disparity_msg_t_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __openni_disparity_msg_t_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int openni_disparity_msg_t_decode_cleanup(openni_disparity_msg_t *p)
{
    return __openni_disparity_msg_t_decode_array_cleanup(p, 1);
}

int __openni_disparity_msg_t_clone_array(const openni_disparity_msg_t *p, openni_disparity_msg_t *q, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __int64_t_clone_array(&(p[element].timestamp), &(q[element].timestamp), 1);

        __int16_t_clone_array(&(p[element].width), &(q[element].width), 1);

        __int16_t_clone_array(&(p[element].height), &(q[element].height), 1);

        __int16_t_clone_array(p[element].pixel_offset, q[element].pixel_offset, 2);

        __int32_t_clone_array(&(p[element].disparity_data_nbytes), &(q[element].disparity_data_nbytes), 1);

        q[element].disparity_data = (uint8_t*) lcm_malloc(sizeof(uint8_t) * q[element].disparity_data_nbytes);
        __byte_clone_array(p[element].disparity_data, q[element].disparity_data, p[element].disparity_data_nbytes);

    }
    return 0;
}

openni_disparity_msg_t *openni_disparity_msg_t_copy(const openni_disparity_msg_t *p)
{
    openni_disparity_msg_t *q = (openni_disparity_msg_t*) malloc(sizeof(openni_disparity_msg_t));
    __openni_disparity_msg_t_clone_array(p, q, 1);
    return q;
}

void openni_disparity_msg_t_destroy(openni_disparity_msg_t *p)
{
    __openni_disparity_msg_t_decode_array_cleanup(p, 1);
    free(p);
}

int openni_disparity_msg_t_publish(lcm_t *lc, const char *channel, const openni_disparity_msg_t *p)
{
      int max_data_size = openni_disparity_msg_t_encoded_size (p);
      uint8_t *buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = openni_disparity_msg_t_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = lcm_publish (lc, channel, buf, data_size);
      free (buf);
      return status;
}

struct _openni_disparity_msg_t_subscription_t {
    openni_disparity_msg_t_handler_t user_handler;
    void *userdata;
    lcm_subscription_t *lc_h;
};
static
void openni_disparity_msg_t_handler_stub (const lcm_recv_buf_t *rbuf,
                            const char *channel, void *userdata)
{
    int status;
    openni_disparity_msg_t p;
    memset(&p, 0, sizeof(openni_disparity_msg_t));
    status = openni_disparity_msg_t_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding openni_disparity_msg_t!!!\n", status);
        return;
    }

    openni_disparity_msg_t_subscription_t *h = (openni_disparity_msg_t_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    openni_disparity_msg_t_decode_cleanup (&p);
}

openni_disparity_msg_t_subscription_t* openni_disparity_msg_t_subscribe (lcm_t *lcm,
                    const char *channel,
                    openni_disparity_msg_t_handler_t f, void *userdata)
{
    openni_disparity_msg_t_subscription_t *n = (openni_disparity_msg_t_subscription_t*)
                       malloc(sizeof(openni_disparity_msg_t_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->lc_h = lcm_subscribe (lcm, channel,
                                 openni_disparity_msg_t_handler_stub, n);
    if (n->lc_h == NULL) {
        fprintf (stderr,"couldn't reg openni_disparity_msg_t LCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int openni_disparity_msg_t_subscription_set_queue_capacity (openni_disparity_msg_t_subscription_t* subs,
                              int num_messages)
{
    return lcm_subscription_set_queue_capacity (subs->lc_h, num_messages);
}

int openni_disparity_msg_t_unsubscribe(lcm_t *lcm, openni_disparity_msg_t_subscription_t* hid)
{
    int status = lcm_unsubscribe (lcm, hid->lc_h);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe openni_disparity_msg_t_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

